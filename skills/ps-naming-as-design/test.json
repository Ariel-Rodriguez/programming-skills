[
    {
        "name": "legacy_code_translation",
        "spec": "Translate this cryptic legacy code into readable English (code):\n\n```javascript\nconst a = [/*...*/];\nfunction f(x) {\n  return x.filter(i => i.s === 1).map(i => i.v);\n}\n```\n\nAssume `a` is a list of Orders, `s` is Status (1=Active), and `v` is Value.\n\nRewrite usage and function to be self-documenting.",
        "expected": {
            "focus": "Do the new names (e.g. `activeOrders`, `orderValue`) explain the *domain concept*?"
        }
    },
    {
        "name": "responsibility_check",
        "spec": "We have a function named `validateUserAndSaveToDatabaseAndSendEmail`. \n\nWhy is this name a \"smell\"? Suggest how you would restructure the code to allow for better naming.",
        "expected": {
            "focus": "Does the answer point out the 'And' implies multiple responsibilities?"
        }
    }
]