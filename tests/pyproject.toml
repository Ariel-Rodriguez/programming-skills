[project]
name = "programming-skills-validation"
version = "0.1.0"
description = "Validation suite for Programming Skills"
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6.0",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "ruff",
]

[tool.uv]
python-downloads = "automatic"
